import diagnosesData from '../../data/diagnoses.json';

const getEntries = () => {
    return diagnosesData;
  };
  
  const addEntry = () => {
    return null;
  };

  export default {
    getEntries,
    addEntry
  };